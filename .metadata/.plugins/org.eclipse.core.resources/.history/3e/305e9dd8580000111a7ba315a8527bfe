#pragma once
#include <cstdint>

enum class CarState : uint8_t
{
    Idle = 0,
    Run,
    Fault
};

struct Telemetry
{
    CarState  state = CarState::Idle;
    uint32_t  uptime_ms = 0;
    uint32_t  last_cmd_age_ms = 0;

    uint16_t  dist_front_mm = 0;
    uint16_t  dist_rear_mm  = 0;
};
